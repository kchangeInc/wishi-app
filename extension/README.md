# Chrome Extension

This folder contains a simple browser extension for importing seller listings from other sites.

- `manifest.json` — extension manifest
- `content.js` — injected script for page scraping and messaging
- `popup.html` / `popup.js` — extension UI to trigger import actions
