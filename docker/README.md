# Docker Setup

Use Docker Compose to build and run the frontend and backend together.

## Start locally

```bash
cd deal-platform
docker-compose up --build
```

- Frontend: http://localhost:3000
- Backend: http://localhost:8000
